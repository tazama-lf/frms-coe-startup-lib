// redis and aragojs is mocked
// setup.jest.js

import { init } from '../lib';

beforeAll(async () => {
  let server = {
    servers: ""
  };
})

describe('init', () =>{
  let natsSpy: jest.SpyInstance;

  beforeEach(() => {
    
    
  })
})